import React from 'react';

export default class Spinning extends React.Component {
    render() {
        return (
            <div className="Spinning"> Process Loading... </div>
        )
    }
};
